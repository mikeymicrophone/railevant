<h1>relevant to rails</h1>

<table>
<% for concept in @concepts %>
  <tr>
    <td><%= link_to concept.class.name.downcase, collection_path(concept) if request.request_uri == '/concepts' %></td>
    <td><%=h concept.content %></td>
    <td><%=h concept.character %></td>
    <td><%= railser_link concept %></td>
    <td><%= link_to 'show', concept %></td>
    <td><%= link_to 'edit', edit_concept_path(concept) %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'conceptualize an abstraction', new_concept_path %>
<%= link_to 'everythang', concepts_path %>